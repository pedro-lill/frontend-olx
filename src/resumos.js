// //cas.trim => Tira os espaços digitados // bom para verificar quantas palavras tem no titulo por exemplo

// // errors.push('Sem título'); // push = adicionar um item no array


// // para verificar se tem imagens e depois para add a imagem e enviar para o backend
// // if(fileField.current.files.length > 0) {
// //     for(let i=0; i<fileField.current.files.length; i++) {
// //         fData.append('img', fileField.current.files[i]);
//     // }
// // }

// history.push(`/ad/${json.id}`); // redireciona para a pagina do anuncio
// return; // para a execução do codigo

// // Um Slug é a parte de identificação exclusiva de um endereço da Web, geralmente no final da URL. 
// // É uma forma de identificar um conteúdo de forma amigável para o usuário, sem que ele precise se preocupar com a estrutura
// //  interna do site.

// FEITO //preciso verificar o que a api esta me mandando, para evitar que de erro quando vem lixo

// FEITO //preciso fazer um link para os anuncios dentro de anuncios 

// FEITO verificiar se o anuncio existe antes de redirecionar

alert( "clicar em um anuncio e jogar o scroll pra cima");
console.log(depois)

alert(" fazer valdiação de login")

// <Link to={`/ad/${props.data._id || props.data.id}`}> 
alert ('a API nao esta padronizada entao a continuação da url pode ser por _id ou id')

alert ('vw pega a tela toda, se colocar 100% o scrool pode atrapalhar, o % pega a largura do doc e o vw pega a da tela')

sibling selector css
